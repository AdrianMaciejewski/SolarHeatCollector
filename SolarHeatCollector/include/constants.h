#ifndef CONSTANTS_H
#define CONSTANTS_H

const float DEFAULT_MIN_ANGLE = 10.0;  // Fully closed
const float DEFAULT_MAX_ANGLE = 90.0;  // Fully open
const int DEFAULT_SPEED = 10;          // Default motor speed in RPM

#endif
